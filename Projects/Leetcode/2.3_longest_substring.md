- Subarray Sum
# Problem
Two Sum

# Brute Force

Idea:

TC:
SC:

# Better Solution

Idea: sliding window using set


TC: O(n)
SC: O(min(n, charset))

# Optimal Solution

Idea: sliding window using hashmap


TC: O(n)
SC: O(min(n, charset))

# Failed Attempts

## Iteration 1

Code:
        # sliding window
        left = 0
        right = 0
        window = set()
        max_size = 0
        while right < len(s):
            if s[right] not in window:
                window.add(s[right])
                right += 1
            else:
                window.remove(s[left])
                max_size = max(max_size, right - left)
                left += 1
        return max_size

Result:
    880 / 988 testcases passed


Failed Case:

Input
s =
" "

Use Testcase
Output
0
Expected
1

Root Cause:


Lesson:

## Iteration 2

Code:

Failed Case:

Root Cause:

Lesson:

## Iteration 3:
class Solution(object):
    def lengthOfLongestSubstring(self, s):
        last_seen = {}
        left = 0
        max_size = 0

        for right, char in enumerate(s):
            if char in last_seen:
                left = max(left, last_seen[char] + 1)

            last_seen[char] = right
            max_size = max(max_size, right - left + 1)

        return max_size

但 HashMap 版本常数因子通常更好，因为遇到重复字符时，可以直接跳跃左边界。

例如：

s = "abcba"

遇到第二个 b 时：

Set 版本：逐个删除 a、b
HashMap 版本：直接令 left = 2

因此更准确地说：

你的代码是最优时间复杂度
last_seen HashMap 通常是更标准、更精炼的最优实现
你的 Set 版本非常适合慢练，因为它更清楚地展示了窗口扩张和收缩的过程

# Final Pattern

Pattern:

Check before Insert

Template:

for x in nums:
    if target-x in map:
        return

    map[x] = index

# Transfer

Similar Problems:

LC 3
↓
LC 904
↓
LC 340
↓
LC 1004
↓
LC 424
↓
LC 567
↓
LC 438
↓
LC 209
↓
LC 76